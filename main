from modelo.Animal import Animal
from modelo.BaseDatos import BaseDatos
from Interfaz.Interface import Interfaz

def main():
    base = BaseDatos()
    base.cargarDatosIniciales(Animal)
    ui = Interfaz(base, Animal)
    ui.iniciar()

if __name__ == "__main__":
    main()

